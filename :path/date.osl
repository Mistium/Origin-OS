// evaluate some js for date stuff
if args[1] == "-l" (
    terminal.writeLine("origin.time.date.toLocaleDateString()".eval().replace("/", "-"))
) else (
    terminal.writeLine("origin.time.date.toString()".eval())
)