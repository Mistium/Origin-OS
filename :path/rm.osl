local target = args[1]
if !target.contains(".") (
  target ++= ".folder"
)
local dir = terminal.resolvePath(target)
file "exists" dir
if exists (
  file "open" dir "onlyaccess"
  file "delete"
  file "close"
) else (
  terminal.writeLine("Unknown target file: " ++ args[1])
)