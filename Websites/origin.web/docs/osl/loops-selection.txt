if condition (
stuff to do if condition == True
)

loop number-of-times (
stuff to loop
)

while condition (
stuff to do
)

until condition (
stuff to do
)

if condition "thing to do if condition == True"

until condition "thing to do"

loop number "thing to do"

while condition "thing to do"

import "file_path"
import "file_name" "file_type"
The "import" command runs another osl file's data.

run ["osl-json"]
The "run" command loops through a json array and runs the commands inside
