code = "input bc code here".ask

code = code.regex("/(?<=\()[^)]+(?=\))|[^ \n()%;]+|[()]|%[^%]*%|;/gm")
loop code.len (
  count += 1
  cur = code.[count]
  last = cur.len
  if cur.[1] == "%" and ( cur.[last] == "%" ) (
    cur = ""
  )
)

cons = "0"
window "show"
mainloop:
c #fff
goto -340 180
text "Console" 10
goto -10 -10
text cons 10
import "win-buttons"
