code = "input bc code here".ask

code = code.regex("/(?<=\()[^)]+(?=\))|[^ \n()%;]+|[()]|%[^%]*%|;/gm")
loop code.len (
  count += 1
  cur = code.[count]
  last = cur.len
  if cur.[1] == "%" and ( cur.[last] == "%" ) (
    cur = ""
  )
)

cons = "> Successfully loaded console. (v0.1)"
window "show"

mainloop:
loggingliney = -340
logginglinex = 180

c #fff
goto loggingliney logginglinex
text "BetterCode Console" 10
loggingliney=+ 20
logginglinex -= 20

goto loggingliney logginglinex
text cons 8
logginglinex -= 20

goto loggingliney logginglinex
text "> finnaly fixed the bug  and i have more lines" 8

import "win-buttons"
