// to stop this program press space

permission "request" "terminal"

mainloop:

display "rotation" "set" (timer * 1000)
if "space".isKeyDown() (
  display "rotation" "set" 0
  window.close()
)

import "win-buttons"
