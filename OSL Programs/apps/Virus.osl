// to stop this program press space

permission "request" "terminal"

mainloop:

display "set" "rotation" + (timer * 1000)
if "space".isKeyDown() (
  display "set" "rotation" 0
  window.close()
)

import "win-buttons"
